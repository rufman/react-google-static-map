{
  "name": "react-google-static-map-2",
  "version": "0.1.0",
  "description": "A wrapper for Google's Static Maps",
  "main": "lib/GoogleStaticMap.js",
  "scripts": {
    "build": "babel GoogleStaticMap.js --out-dir lib",
    "clean": "rimraf lib dist",
    "prepublish": "npm run clean && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:dwr/react-google-static-map.git"
  },
  "files": [
    "GoogleStaticMap.js",
    "README.md"
  ],
  "keywords": [
    "react-component"
  ],
  "author": "Stephane Rufer <stephane.rufer@gmail.com>",
  "peerDependencies": {
    "react": ">=0.14"
  },
  "devDependencies": {
    "babel-cli": "^6.9.0",
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-react": "^6.5.0",
    "rimraf": "^2.5.2"
  }
}
